# FI-TEAM-COHESION-B2.2 — Directory, Access & Onboarding physical consolidation

**Status:** GREEN candidate — B2.2a–c complete (B2.2d optional deferred)  
**Predecessor:** [b2.1b-identity-server-consolidation.md](./b2.1b-identity-server-consolidation.md)  
**Date:** 2026-08-06

## Goal

Physically home directory, access, and onboarding implementations under
`src/lib/team/{directory,access,onboarding}` with `server.ts` barrels.
Break the remaining onboarding invite↔PIN cycle. Keep hire-invite vs login-invite
semantics forever separate (collision C9).

Identity-proof projections (B1.1–B1.3) already live in `team/*`; this slice moves
the remaining legacy loaders, cores, and domain mutation servers.

## Preconditions (GREEN)

- B2.1b identity pure + server under `team/identity`; **0** identity shims
- Dual-table allowlist **15**; `cycleCount: 1` (invite↔PIN only)
- Directory / access / onboarding centre loaders already consume `resolveStaffIdentities`
- B3 sprint-action renames deferred

## Out of scope

- B3 action renames
- Identity mutation servers (`staffLifecycle.server`, HR reconciliation, merge/repair)
- `staffTenantLinkRepair.server.ts` — remains the explicit dual-table repair boundary
- Merging `onboardingInvitation` with `staffAccessInvite` (hire invite ≠ login invite)

## Current shells

| Domain | Today | Missing |
|--------|-------|---------|
| `team/directory` | Pure + server barrel (B2.2a) | Deferred hot cluster (B2.2d) |
| `team/access` | Pure + mutations + `server.ts` (B2.2b) | Action file move (B3); `staffFiUserLink.server` |
| `team/onboarding` | Pure + mutations + `server.ts` + cycle break (B2.2c) | Action file move (B3) |

## Sub-slice sequence

### B2.2a — Directory core

**Status:** GREEN candidate (2026-08-06)

**Moved**

- `staffDirectoryLoader.server.ts`, `staffDirectoryFilters.ts` (+ identity/lifecycle tests)
- `workforceOsDirectoryLoader.server.ts`
- `staffAssigneeDisplay.ts`, `calendarVisibleStaff*`
- `clinicalStaffPickerLoader.server.ts`, `assertStaffClinicallyAvailable.server.ts`

**Added** `team/directory/server.ts` — page loaders, OS directory/HR recon/staff profile loaders, clinical picker loader, availability asserts.

**Deferred to B2.2d:** `clinicalStaffPicker.ts` (~36 consumers), `staffHrNotificationSummary*`, role/hours cluster.

**Shim policy:** move + full re-point; **0** shims.

### B2.2b — Access pure + mutations

**Status:** GREEN candidate (2026-08-06)

**Moved (flat under `team/access`, filenames preserved)**

Pure:

- `staffAccessCentreCore*`
- `staffAccessInviteCore*`
- `staffAccessAcceptCore.ts`
- `workforceHrManageGateCore` (+ test)
- `staffHrTaskMapCore*` / `staffHrTaskMapBannerCore*`
- `staffFiUserLinkPlan*`

Mutations / servers:

- `staffAccessCentre.server.ts`
- `staffAccessAccept.server.ts` (+ test)
- `staffAccessInviteAudit.server.ts`
- `staffAccessPinLayer.server.ts`
- `workforceHrManageGate.server.ts`

**Added** `team/access/server.ts` — centre loader, accept/PIN, manage gate, audit.

**Shim policy:** move + full re-point; **0** shims. No legacy re-export stubs.

**Deferred:** `staffFiUserLink.server.ts`; B3 `workforce-staff-access-actions` rename (actions remain temporarily registered under `src/lib/actions`).

**Allowlist:** 15 → **13** (accept + PIN servers now under `team/access`, domain-exempt; repair kept).

**Token-flow boundary:** login invite ↔ access PIN dynamic import left intact; onboarding invite↔PIN cycle deferred to B2.2c (`cycleCount` still **1**).

### B2.2c — Onboarding + cycle break

**Status:** GREEN candidate (2026-08-06)

**Cycle broken (one-way leaves)**

```
onboardingInvitation.server → onboardingPinSetup.server (create/load)
onboardingPinLayer.server   → onboardingInvitationAccept.server (accept)
```

DAG: `invitation → pinSetup`, `pinLayer → invitationAccept` → **`cycleCount: 0`**.

Shared invite hashing continues to use access `staffAccessInviteCore` acyclically — hire invite ≠ login invite (C9).

**Moved (flat under `team/onboarding`)**

- Types / URL / centre cores + tests
- Page loader, checklist server
- Invitation send/load + accept leaf + PIN setup leaf + PIN complete layer
- Staff-create core/server (+ tests)
- Status helper: `onboardingInviteStatusCore` (deduped hire-invite status)

**Added** `team/onboarding/server.ts`.

**Shim policy:** move + full re-point; **0** shims.

**Allowlist:** 13 → **12** (invitation server under `team/onboarding`, domain-exempt; repair kept).

**Deferred:** B3 `workforce-onboarding-actions` rename.

### B2.2d (optional) — Directory hot deferrals

- `clinicalStaffPicker` cluster, `staffHrNotification*`, role/hours cluster
- Prefer move + full re-point; optional one-PR shim only if consumer count exceeds budget

## Shim policy

Follow **B2.1b**: move implementation + full consumer re-point in the same slice; **0 long-lived shims**.

Optional exception only for deferred directory hot surfaces (`clinicalStaffPicker`) if bulk re-point exceeds budget — document and delete in B2.2d / B2.2.x.

## Allowlist

| Path | Action |
|------|--------|
| `workforce/onboarding/onboardingInvitation.server.ts` | **Removed** (B2.2c → `team/onboarding`, exempt) |
| `workforce/staffAccessAccept.server.ts` | **Removed** (B2.2b → `team/access`, exempt) |
| `workforce/staffAccessPinLayer.server.ts` | **Removed** (B2.2b → `team/access`, exempt) |
| `workforce/staffTenantLinkRepair.server.ts` | Keep |

**After B2.2c: 12** (locked-plan target met).

## Public API additions

| Domain | Pure (`index.ts`) | Server (`server.ts`) |
|--------|-------------------|----------------------|
| directory | Existing projection + filters | Page / OS / clinical loaders |
| access | Projection + centre/task-map/link-plan cores | Centre loader, manage gate, accept/PIN, audit |
| onboarding | Existing projection | Page loader, invite URL builders (B2.2c) |

Token hashing stays on `staffAccessInviteCore` (Node crypto; server routes). Repair stays on `staffTenantLinkRepair.server`.

## Scorecard (post–B2.2c inventory)

| Metric | B2.1b | B2.2b | B2.2c now |
|--------|------:|------:|----------:|
| Legacy tree files (`workforce-os`+`workforce`+`staff`) | 260 | 241 | **228** |
| Deep legacy imports | 253 | 251 | **247** |
| Documented cycles | 1 | 1 | **0** |
| Temporary shims | 0 | 0 | **0** |
| Identity / dual-table allowlist | 15 | 13 | **12** |
| Sprint action renames | 7 | 7 | 7 |

## Acceptance

- [x] Directory / access / onboarding implementations under `team/{directory,access,onboarding}` for MUST-MOVE set
- [x] `team/*/server.ts` barrels; consumers import domain index / server
- [x] `cycleCount: 0`
- [x] Allowlist ≤ 12 and must not grow (12)
- [x] 0 temporary shims
- [x] Onboarding vs login invite behaviour unchanged (token URLs / cores preserved)
- [x] Inventory regenerated; architecture + onboarding + route-contract tests green

## Test plan

- Existing: `staffDirectory*`, `staffAccess*`, `onboarding*`, bridge tests
- Token routes: onboarding invite accept, staff-access accept, PIN setup (hire + login keep distinct)
- Regression: B1.1–B1.3 identity-proof scenarios after path moves
- Prefer Node scripts over PowerShell nested-array `.Replace` for bulk path edits (B2.1b recovery lesson)

## Risk register

| Risk | Mitigation |
|------|------------|
| `clinicalStaffPicker` ~36 consumers | Defer to B2.2d or bulk codemod |
| Cross-domain token helper import | Keep acyclic; never merge C9 |
| Dual-table writes in moved mutations | Allowlist shrink only; repair stays deferred; `team/access` domain-exempt |
| Access pure still imports legacy core | Fixed in B2.2b |

## Recommended next

**B2.2d — `clinicalStaffPicker` hotspot** (~36 consumers), optional directory cleanup.
