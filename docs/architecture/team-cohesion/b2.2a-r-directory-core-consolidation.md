# FI-TEAM-COHESION-B2.2a-R — Directory Core Consolidation

**Status:** PLANNED (next)  
**Predecessor:** [b2.2-directory-access-onboarding.md](./b2.2-directory-access-onboarding.md) (B2.2b–d GREEN; original B2.2a **not delivered**)  
**Date:** 2026-08-06  
**Naming:** `B2.2a-R` = recovery of the directory-core physical move that earlier docs overstated as GREEN.

## Delivery-history correction (required)

Earlier B2.2a documentation overstated delivery. The clinical picker support surface
was partially prepared in planning text, but the identified **directory-core
loaders remained in their legacy locations**. No `git` commit ever moved them.

| Claim (pre-correction) | Actual |
|------------------------|--------|
| B2.2a Directory core: GREEN | **NOT DELIVERED** — files still under `src/lib/staff/` / `workforce-os/` |
| `clinicalStaffPickerLoader` moved in B2.2a | Loader moved later as part of **B2.2d** only |
| `team/directory/server.ts` covers page / OS / availability loaders | After B2.2d, `server.ts` exports **clinical picker loaders only** |

**B2.2d** completed the `clinicalStaffPicker` hotspot only (`e3d5a55c`).  
**Directory-core physical consolidation** is this slice: **B2.2a-R**.

## Objective

Move the actual directory-owned implementations into the canonical Team Directory
boundary, migrate consumers onto public barrels, and leave **0** legacy
directory-core paths — without changing filtering, visibility, or availability
behaviour.

## Canonical boundaries

| Barrel | Use |
|--------|-----|
| `@/src/lib/team/directory` | Pure types, projections, filters, assignee/visibility helpers |
| `@/src/lib/team/directory/server` | Page / OS directory loaders, clinical availability asserts |

No compatibility shims. Prefer Node / AST import rewriting (B2.1b / B2.2d lesson).

## Scope — must-move set

Confirm against regenerated `generated/b0-inventory.csv` (`proposedDomain=directory`
rows still under legacy trees) before starting. Expected core:

| Current (legacy) | Proposed |
|------------------|----------|
| `src/lib/staff/staffDirectoryLoader.server.ts` | `team/directory/staffDirectoryLoader.server.ts` |
| `src/lib/staff/staffDirectoryFilters.ts` | `team/directory/staffDirectoryFilters.ts` |
| `src/lib/staff/staffDirectoryIdentityAttention.test.ts` | co-locate |
| `src/lib/staff/staffDirectoryLifecycle.test.ts` | co-locate |
| `src/lib/workforce-os/workforceOsDirectoryLoader.server.ts` | `team/directory/workforceOsDirectoryLoader.server.ts` |
| `src/lib/staff/staffAssigneeDisplay.ts` | `team/directory/staffAssigneeDisplay.ts` |
| `src/lib/staff/calendarVisibleStaff.ts` (+ test) | `team/directory/calendarVisibleStaff*` |
| `src/lib/staff/assertStaffClinicallyAvailable.server.ts` | `team/directory/assertStaffClinicallyAvailable.server.ts` |

Plus any other B0-classified directory-core loaders / projections / helpers still
in legacy trees that are required for barrel completeness **excluding** the
optional hot cluster below.

### Explicitly out of B2.2a-R (next hot cluster)

Defer unless inventory shows they must move with the core to avoid a broken barrel:

- `staffHrNotificationSummary*` / `staffHrNotificationLoader*`
- role / hours cluster (`staffRolePolicy*`, `staffRoleReview*`, `staffWeeklyHours*`, `staffSlotHours*`)
- `myHrPortal*` / `staffHrImportPage*` (directory-adjacent; schedule separately by fan-out)

## Preserve

- tenant and clinic isolation
- directory filtering and ordering
- active / inactive lifecycle treatment
- scheduling-only visibility
- clinical availability checks
- calendar-visible staff behaviour
- no lifecycle-only rows added to the default directory
- no new dual-table joins
- existing DTO and UI behaviour
- allowlist ≤ 12; `cycleCount: 0`

## Migration method

1. `git mv` implementations + co-located tests into `src/lib/team/directory/`.
2. Expand `team/directory/index.ts` (pure) and `team/directory/server.ts` (loaders / asserts).
3. Node rewrite all consumers to barrels — no deep legacy imports; avoid exposing internal modules solely to preserve old deep paths.
4. Delete legacy sources only when runtime + test + architecture path strings are zero.
5. Run nearest domain tests after each consumer group (directory, calendar, booking, roster, architecture).
6. Regenerate inventory + B2 scorecard; correct any remaining overstated B2.2a wording.

## Acceptance criteria

B2.2a-R is **GREEN** when:

- [ ] all B0-classified directory-core implementations live under `team/directory`
- [ ] pages and domain consumers use public Directory barrels
- [ ] no duplicate implementations remain
- [ ] no temporary shims are introduced
- [ ] no legacy directory-core import path strings remain in source or tests
- [ ] cycles stay at **0**
- [ ] allowlist remains **≤ 12**
- [ ] related directory, calendar, booking, roster and architecture tests pass
- [ ] inventory and scorecard are regenerated
- [ ] prior documentation remains corrected to reflect actual delivery history (B2.2a not delivered; B2.2a-R recovers it)

## Programme order after B2.2a-R

Do **not** start B3 sprint-action renames until directory homes stabilize.

1. Complete **B2.2a-R** (this slice).
2. Move the next highest-fan-out legacy domain cluster (B0 import concentration).
3. Stabilise that domain’s public / server boundaries.
4. Then B3 sprint-action renames against stable destinations.

## Related

- Parent programme record: [b2.2-directory-access-onboarding.md](./b2.2-directory-access-onboarding.md)
- Register: [README.md](./README.md)
- Ownership: [domain-ownership.md](./domain-ownership.md)
