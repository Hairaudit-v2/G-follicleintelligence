# FI-TEAM-COHESION-B2.2a — Directory Core Consolidation

**Status:** GREEN (2026-08-06)  
**Also known as:** B2.2a-R (recovery id used when earlier docs overstated B2.2a delivery)  
**Predecessor:** [b2.2-directory-access-onboarding.md](./b2.2-directory-access-onboarding.md)  
**Land:** with B2.2 programme docs on `main`

## Delivery-history note

Earlier planning text once claimed B2.2a GREEN without a physical move. That claim was **incorrect** and was stricken. This document is the delivery record for the **actual** directory-core consolidation. B2.2d remained a separate clinical-picker hotspot (`e3d5a55c`).

## Objective (delivered)

Moved directory-owned loading, filtering, calendar-visible predicates, assignee labels, and single-person clinical availability asserts into `src/lib/team/directory`, with consumers on public barrels and **0** shims.

## Canonical boundaries

| Barrel | Surface |
|--------|---------|
| `@/src/lib/team/directory` | Types, projections, filters, calendar-visible predicates, assignee labels, clinical picker helpers |
| `@/src/lib/team/directory/server` | Directory page / OS loaders, clinical picker loaders, clinical availability asserts |

**Cycle break:** `staff.server` imports `assertStaffClinicallyAvailableForAssignment` from the deep module  
`@/src/lib/team/directory/assertStaffClinicallyAvailable.server` so the unified `server` barrel does not pull `staffDirectoryLoader` into a cycle with `staff.server`. Other assert consumers may use `@/src/lib/team/directory/server`.

## Moved (filenames preserved)

| From | To |
|------|----|
| `staff/staffDirectoryLoader.server.ts` | `team/directory/staffDirectoryLoader.server.ts` |
| `staff/staffDirectoryFilters.ts` (+ identity/lifecycle tests) | `team/directory/…` |
| `workforce-os/workforceOsDirectoryLoader.server.ts` | `team/directory/workforceOsDirectoryLoader.server.ts` |
| `staff/calendarVisibleStaff.ts` (+ test) | `team/directory/…` |
| `staff/staffAssigneeDisplay.ts` | `team/directory/staffAssigneeDisplay.ts` |
| `staff/assertStaffClinicallyAvailable.server.ts` | `team/directory/assertStaffClinicallyAvailable.server.ts` |

**Migration:** Node rewrite `scripts/team-cohesion/b2.2a-rewrite-directory-core-imports.mjs`.

## Explicitly deferred (post–B2.2a hot cluster)

- `staffHrNotificationSummary*` / `staffHrNotificationLoader*`
- role / hours cluster (`staffRolePolicy*`, `staffRoleReview*`, `staffWeeklyHours*`, `staffSlotHours*`)
- `myHrPortal*` / `staffHrImportPage*`
- Roster eligibility, access/onboarding, compliance policy, procedure staffing optimisation

## Preserved

tenant/clinic filtering; scheduling-only visibility; active/inactive behaviour; default directory exclusion of lifecycle-only people; clinical availability rules; calendar-visible ordering; existing DTOs; no new identity joins; no N+1; allowlist ≤ 12; `cycleCount: 0`.

## Acceptance

- [x] Directory-core implementations under `src/lib/team/directory`
- [x] No duplicate legacy implementation for the must-move set
- [x] Consumers use public barrels (except documented `staff.server` deep assert import)
- [x] Shims = 0
- [x] Cycles = 0
- [x] Allowlist ≤ 12
- [x] Legacy import count decreased (211 → 199 deep violations; staff tree 36 → 28)
- [x] Directory / calendar / booking / roster / architecture nearest tests pass
- [x] Inventory regenerated; docs no longer claim B2.2a was previously delivered — this slice **is** the delivery

## Scorecard delta (B2.2d → B2.2a)

| Metric | B2.2d | B2.2a |
|--------|------:|------:|
| Legacy tree files | 225 | **216** |
| Deep legacy imports | 211 | **199** |
| Cycles | 0 | **0** |
| Shims | 0 | **0** |
| Dual-table allowlist | 12 | **12** |

## Recommended next

1. Inspect regenerated B0 inventory for the next highest-fan-out legacy cluster.
2. Stabilise that domain’s public/server boundaries.
3. **Then** B3 sprint-action renames into final homes — not before.
