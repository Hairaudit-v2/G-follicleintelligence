# FI-TEAM-COHESION-B2.3a — Staff Role Policy proof

**Status:** GREEN (2026-08-06)  
**Plan:** [b2.3a-staff-role-policy-plan.md](./b2.3a-staff-role-policy-plan.md)  
**Commit message target:** `FI-TEAM-COHESION-B2.3a — Staff role policy consolidation`

## Discovery summary

Regenerated B0 inventory after B2.2a. Highest remaining fan-out clusters included `staff.server` (identity CRUD), large roster/identity remaining sets, and the deferred role/hours hotspot.

The role/hours hotspot **must not move as one cluster** — it mixes:

- clinical role bookability (`staffRolePolicy`) → **directory**
- HR notification DTOs → **not directory** (identity readiness already consumes contracts)
- employment weekly hours + booking slot gate → deferred semantic split
- payroll role-review workflow → deferred mixed composition

**Selected cluster:** `staffRolePolicy` only — clear directory ownership, behaviour-neutral, no shims, no identity→directory risk.

## Delivered

| From | To |
|------|----|
| `src/lib/staff/staffRolePolicy.ts` | `src/lib/team/directory/staffRolePolicy.ts` |

- Public exports on `@/src/lib/team/directory`
- Directory-internal deep sibling imports avoid barrel cycles
- Role-policy unit tests → `team/directory/staffRolePolicy.test.ts`
- Legacy path deleted (no shim)
- Rewrite: `scripts/team-cohesion/b2.3a-rewrite-staff-role-policy-imports.mjs`

## Consumers repointed (sample)

External → `@/src/lib/team/directory`: twin page, `StaffDirectoryClient`, `StaffRoleReviewClient`, CRM/calendar/bookings/clinic setup, `hrStaffReadinessDashboard*`, `staffRoleReviewApply`, `staffSlotHours.server`.

Internal deep → `@/src/lib/team/directory/staffRolePolicy`: `staffDirectoryFilters`, `clinicalStaffPicker`, `assertStaffClinicallyAvailable.server`, related tests.

## Deliberately deferred

| Files | Why |
|-------|-----|
| `staffHrNotification*` / `myHrPortal*` | Identity readiness consumes stale-day / summary contracts — not directory |
| `staffWeeklyHours` / `staffSlotHours` | Employment vs roster vs booking ownership unresolved |
| `staffRoleReview*` + sprint actions | Mixed payroll + clinic + HR notification composition |
| `staffPayrollSourceDisplay` / `staffProfileExtras` | Not role-policy |

## Verification

| Check | Result |
|-------|--------|
| `staffRolePolicy.test.ts` + directory / HR / bookings / calendar consumers | **52** pass (nearest suite) |
| `staffRoleReview.test.ts` + `calendarVisibleStaff.test.ts` | **10** pass |
| `staffIdentityArchitecture.static.test.ts` | pass · allowlist **12** |
| Inventory generator | `cycleCount: 0` · staff tree **27** · deep **195** |
| `tsc --noEmit` | No **new** errors on moved module / consumers (pre-existing failures elsewhere unchanged) |

## Scorecard

| Metric | B2.2a | B2.3a |
|--------|------:|------:|
| Legacy tree files | 216 | **215** |
| Deep legacy imports | 199 | **195** |
| Cycles | 0 | **0** |
| Shims | 0 | **0** |
| Dual-table allowlist | 12 | **12** |

Legacy = `workforce-os` (76) + `workforce` (112) + `staff` (27).

## Acceptance

- [x] One `staffRolePolicy` implementation under `team/directory`
- [x] Consumers on Directory public barrel (internals use documented deep sibling)
- [x] No legacy duplicate / no shim
- [x] Cycles 0 · allowlist ≤ 12
- [x] Behaviour unchanged for role visibility / clinical bookability
- [x] Inventory + architecture README updated
EOF
