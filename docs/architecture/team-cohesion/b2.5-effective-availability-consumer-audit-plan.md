# FI-TEAM-COHESION-B2.5 — Effective availability consumer audit + explanation UI

**Status:** LOCKED for delivery  
**Date:** 2026-08-06  
**Predecessor:** B2.4 Availability + booking slot gate GREEN (`80c306c1`)

## Locked contracts (unchanged)

| Concern | Owner | Surface |
|---------|-------|---------|
| Recurring weekly template | Team Roster Availability | `@/src/lib/team/roster/availability` |
| Effective UTC-range availability | Team Roster Availability | `getStaffAvailabilityForRange` |
| Final appointment allow/deny | Bookings | `staffSlotAvailability.server.ts` |
| Directory bookability | Directory | role / active / clinic only |
| Payroll contracted hours | Payroll | `fi_staff_standard_hours` (unchanged) |

**Precedence:** blocking leave/unavailable/training/admin/maternity → `available_override` → weekly template.

## Phase 1 — Consumer audit summary

### Must change this slice

| File / function | Class | Current | Canonical | Ignores leave / override? | Risk |
|-----------------|-------|---------|-----------|---------------------------|------|
| `calendar/findNextAvailableBookingSlots.server.ts` · `findNextAvailableBookingSlots` | 4 slot finding | weekly window only | `getStaffAvailabilityForRange` (+ load blocks) | **yes / yes** | high |
| Surgery / smart-scheduling / actions wrapping findNext | 4 | inherits | same | yes / yes | high |
| `rosterGenerationCore` · `BLOCKING_BLOCK_TYPES` | 2 planning | local duplicate Set | import `BLOCKING_AVAILABILITY_BLOCK_TYPES` | n/a | medium |
| Roster CC grid + `RosterShiftDrawer` | 2 UI | block label chips only | shared explanation result | shows leave; no why for weekly/override | low |
| `effectiveAvailability` result | 3 API | `available` + `reasons[]` | add structured explanation | n/a | low |

### Already correct (no behaviour change)

| Consumer | Class | Contract |
|----------|-------|----------|
| `assertStaffAppointmentWithinWorkingHours` + bookings / appointmentsApi / conflict preview / resource requirements | 5 final booking | Bookings gate |
| Engine / candidates / CC event ranking | 3 effective | `getStaffAvailabilityForRange` |
| Staff weekly editors / directory persist | 1 template | weeklyHours |
| HR readiness / clinical picker “hours configured?” | directory readiness | not a time-window gate |
| `staffStandardHours*` | 6 payroll | separate |

### Stale but deferred (document only)

| Item | Why deferred |
|------|--------------|
| CalendarOS `deriveWorkforceBlocksForStaffRow` without DB leave blocks | Visual day chrome; needs loader plumbing for `fi_staff_availability_blocks`; not final booking path |
| `workforceEventAssignmentBridge` conflict overlay without weekly/override | Soft conflict detector; candidates already use effective availability — hard product parity follow-up |
| Appointment create schedule **hint** (weekly-only) | Soft UX tip; write path already uses Bookings gate |
| `available_override` create CTA in Roster CC | API exists; product control remains optional — explanation for existing overrides ships |
| `staffRoleReview*` / sprint actions | Forbidden move |
| Full workforceRostering / standard-hours home move | Larger roster consolidation |

### Slot-finder policy

**Decision (no stop required):** Suggestions must match save-time Gate precedence. `findNextAvailableBookingSlots` loads overlapping active blocks for the horizon and filters with `getStaffAvailabilityForRange`. Weekly hours alone must never allow a suggested slot that booking would deny, and must never hide a slot allowed only via `available_override`.

## Phase 2 — Delivery

1. Add shared `StaffAvailabilityExplanation` on effective availability (`available`, `source`, `reason`, `overrideType`, `blockingRecordId`, `effectiveStart` / `effectiveEnd`).
2. Repoint slot finder to effective availability.
3. Deduplicate generation blocking types import.
4. Surface explanation in Roster Command Centre cells + shift drawer (weekly / override / leave / sick / unavailable / outside hours).
5. Preserve manage gates + rosterTx; no shims; no new cycles; no dual-table joins; no staffRoleReview moves.

## Verification

- roster availability tests
- booking slot / gate core tests
- slot-finder tests (new)
- calendar assignment / resource model tests
- roster command-centre tests
- architecture tests + inventory generator
- `tsc --noEmit` — report only new errors
