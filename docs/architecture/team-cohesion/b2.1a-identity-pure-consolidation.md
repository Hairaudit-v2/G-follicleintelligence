# FI-TEAM-COHESION-B2.1a — Identity pure-layer physical consolidation

**Status:** GREEN (2026-08-05) — shims removed in B2.1b  
**Predecessor:** [b1-identity-program-closure.md](./b1-identity-program-closure.md)  
**Scope:** Pure / types only — server resolve/audit/links completed in [B2.1b](./b2.1b-identity-server-consolidation.md)

## What moved

Implementations now live under `src/lib/team/identity/` (filenames preserved):

| Module | Notes |
|--------|-------|
| `staffLifecycleTypes.ts` | Employment enums / lifecycle row types |
| `staffEmploymentStatusPredicates.ts` | **New leaf** — cycle break |
| `staffLifecycleCore.ts` | Profile editability + reconciliation suggestions |
| `staffLifecyclePresentation.ts` | Presentation mapping |
| `staffCanonicalLifecycle.ts` | Canonical status combiner |
| `workforceIdentityMetadata.ts` | Source-id metadata contract |
| `workforceIdentitySources.ts` | Canonical source_system values |
| `workforceIdentitySummary.ts` | Identity link summary projection |
| `workforceIdentityReadinessSignals.ts` | Readiness signal projection |
| `workforceReadinessBands.ts` | Score → band map |
| `workforceReadinessEngine.ts` | Multi-factor readiness score |
| `workforceReadinessClinicalEligibility.ts` | Clinical assignment gate |
| Matching `*.test.ts` | Co-located under `team/identity/` |

## Cycle break

Documented B0 cycle `staffLifecycleCore` ↔ `hrReconciliationEligibleCore` is **broken**:

- Leaf: `staffEmploymentStatusPredicates.ts` (`parseStaffEmploymentStatus`, `isOperationallyIneligible`, …)
- `hrReconciliationEligibleCore` imports the leaf (not `staffLifecycleCore`)
- `staffLifecycleCore` may still import HR archival / link helpers one-way

## Temporary compatibility exports

~~Legacy basenames under `src/lib/workforce-os/`~~ — **removed in B2.1b**.

Public API additions on `@/src/lib/team/identity`: lifecycle constants, employment predicates, `resolveCanonicalStaffLifecycleStatus`.

## Still deferred (B2.1b+)

- `workforceIdentityLinks.server.ts`, readiness / identity tenant overview servers
- `staffIdentityReadinessAudit.server.ts`, `staffIdentityAuditAccess.server.ts`
- `staffLifecycle.server.ts`, offboarding / reconciliation mutation servers
- Consumer re-point away from shims
- Allowlist reduction (must not grow; still **16**)

## Exit check (this slice)

- [x] Pure identity implementations under `team/identity`
- [x] No duplicate pure implementation remaining (legacy = shim only)
- [x] Documented lifecycle↔HR eligibility cycle broken
- [x] Compatibility exports documented and temporary
- [x] Allowlist ≤ 16
- [x] Identity + architecture tests green (96 pass)
- [x] Inventory regenerated (`cycleCount: 1`, identity domain files consolidated)
