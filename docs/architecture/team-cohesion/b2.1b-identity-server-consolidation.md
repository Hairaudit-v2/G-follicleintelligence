# FI-TEAM-COHESION-B2.1b — Identity server consolidation

**Status:** GREEN candidate (2026-08-05)  
**Predecessor:** [b2.1a-identity-pure-consolidation.md](./b2.1a-identity-pure-consolidation.md)  
**Branch:** `cursor/team-identity-server-b21b`

## What moved

Identity-owned **server** loaders now live under `src/lib/team/identity/` (filenames preserved):

| Module | Notes |
|--------|-------|
| `workforceIdentityLinks.server.ts` | `fi_staff_source_ids` link CRUD / resolve |
| `staffIdentityReadinessAudit.server.ts` (+ test) | Read-only dual-table readiness audit |
| `staffIdentityAuditAccess.server.ts` | HR OS route-aligned audit gate |
| `workforceIdentityTenantOverview.server.ts` | Tenant identity coverage aggregates |
| `workforceReadinessTenantOverview.server.ts` | Tenant readiness aggregates |
| `workforceIdentityLinks.pure.test.ts` | Confirms summary/signals without `server-only` |

## Public server API

`@/src/lib/team/identity/server` re-exports resolvers, links, readiness audit runs, and tenant overviews.

**Deliberate split:** `resolveStaffIdentityAuditAccess` stays on
`@/src/lib/team/identity/staffIdentityAuditAccess.server` so the main server barrel does not pull React `cache` / HR OS route-gate into identity unit tests and other barrel consumers.

Client components keep **type-only** imports from
`staffIdentityReadinessAudit.server` (not the `server` barrel).

## Shim deletion

All **11** B2.1a `workforce-os/<basename>.ts` compatibility re-exports were deleted after consumers were re-pointed to `@/src/lib/team/identity/*`.

## Allowlist

- Before: **16**
- Removed: `src/lib/workforce-os/staffIdentityReadinessAudit.server.ts` (now under `team/identity`, exempt)
- After: **15** (must not grow)

## Explicitly deferred

- `staffLifecycle.server.ts`, `hrReconciliation.server.ts`, offboarding / merge / repair mutations
- `staffTenantLinkRepair.server.ts` — remains the explicit dual-table repair boundary
- `staff/staff.server.ts` hot CRUD surface
- Onboarding invite ↔ PIN cycle (untouched; still the single documented cycle)

## Exit check

- [x] Identity-owned server implementations under `team/identity`
- [x] No identity implementation for moved modules remains under `workforce-os` / `workforce` / `staff`
- [x] Consumers use `team/identity` / `team/identity/server` (access gate: dedicated server path)
- [x] All 11 B2.1a shims deleted
- [x] Cycles ≤ 1 (invite↔PIN only)
- [x] Allowlist ≤ 16 (15)
- [x] Identity + architecture tests green (93 pass)
- [x] Inventory regenerated
