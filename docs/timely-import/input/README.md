# Timely ServiceSales export (optional)

Place `ServiceSales.csv` here to use it as the default input for `npm run timely:import-preview`.

The filename `ServiceSales.csv` is gitignored so real exports are not committed by accident.
