# Certification runs

Each completed FI-TRICHOSCOPY-1A.1 execution gets a folder here:

`runs/<YYYYMMDDTHHMMSS>-staging/`

Create with:

```bash
npm run certify:trichoscopy-1a1 -- --init-run
```

Do not commit secrets, raw PHI, or unredacted payloads.
