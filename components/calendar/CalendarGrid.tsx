"use client";

/**
 * Time-grid calendar (Day / 3 Day / Week) — wraps {@link WeekView} for the CRM shell.
 */
export { WeekView as CalendarGrid, type WeekViewProps as CalendarGridProps } from "@/components/calendar/WeekView";
